import React from 'react';
import Button from '@material-ui/core/Button';

const GreenButton = () => {
  return(
    <Button 
    variant="outlined" style={{backgroundColor: '#FFFFFF', color: '#4caf50'}}
    >Select Athlete</Button>
  )
}

export default GreenButton;